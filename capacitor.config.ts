import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-vue-eslint-prettier-starter',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
